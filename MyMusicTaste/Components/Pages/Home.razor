@page "/"
@using MongoDB.Bson
@using MongoDB.Driver
@using MyMusicTaste.Components.Forms
@using MyMusicTaste.Database

<PageTitle>Home</PageTitle>

@foreach (var doc in dbs)
{
    <p>@doc.ToString()</p>
}

@code {
    private List<BsonDocument> dbs = MongoDbConnection.Client.ListDatabases().ToList();
}

Welcome to your new app.