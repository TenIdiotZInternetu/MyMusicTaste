@page "/debug"
@using MyMusicTaste.Database
@using MyMusicTaste.Models
@using MyMusicTaste.Components.SearchComps

@rendermode InteractiveServer

@inject IDbRepository<Song> Repo

<SongSearch @ref="Search" ResultsCount="10"/>

@code 
{
    private SongSearch Search { get; set; } = new();
    
    protected override void OnAfterRender(bool _)
    {
        Search.UpdateResults("Lo");
    }
}